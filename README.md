# Privacy-Preserving Fraud Detection using Federated Learning

Final year project: fraud detection trained collaboratively across simulated
institutions using federated learning (FedAvg, FedProx) with differential
privacy, benchmarked against a centralized baseline.

## Setup
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
